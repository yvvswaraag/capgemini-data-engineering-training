so here the queries are based on the pyspark data transformations especially focusing on join and aggregation functions.


What I Learned:
How to join two DataFrames using common keys.
Performing aggregations like total, average, and count.
Using groupBy() for data analysis.
Filtering grouped results.
Sorting data based on computed metrics.
Handling missing data using joins (like customers with no orders).

Here the challenges I faced are:
1.writing the aggregation logic correctly.
2.Combining multiple operations in a single query.
