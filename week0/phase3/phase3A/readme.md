# Phase 3A – Data Cleaning in PySpark

##  What I Learned
In this phase, I learned the importance of cleaning data before analysis. I understood how to handle missing values, remove duplicates, and correct invalid data to ensure accuracy.

## What I Did
- Explored the dataset using `show()` and `printSchema()`
- Identified issues like null values, duplicates, and incorrect age values
- Removed rows with missing `customer_id`
- Filled missing `name` and `city` with `"Unknown"`
- Removed duplicate records
- Filtered invalid age values
- Compared data before and after cleaning
- Counted customers in each city

##  Final Outcome
Successfully cleaned the dataset and made it ready for analysis. Learned that proper data cleaning is essential for reliable results.
